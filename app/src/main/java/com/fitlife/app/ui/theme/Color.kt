package com.fitlife.app.ui.theme

import androidx.compose.ui.graphics.Color

// Primary - Electric Green (energy, performance)
val Green80 = Color(0xFFB5F265)
val Green60 = Color(0xFF8FD832)
val Green40 = Color(0xFF5CB800)

// Secondary - Steel Blue (trust, technology)
val Steel80 = Color(0xFFB0C8E8)
val Steel60 = Color(0xFF7DA4CC)
val Steel40 = Color(0xFF4A7FAF)

// Accent - Burn Orange (intensity, motivation)
val Orange80 = Color(0xFFFFB574)
val Orange60 = Color(0xFFFF8C2A)
val Orange40 = Color(0xFFD96B00)

// Neutrals - Dark Mode Base
val CharcoalDark  = Color(0xFF0D0F12)   // deepest background
val CharcoalMid   = Color(0xFF161A1F)   // surface / card
val CharcoalLight = Color(0xFF1E252D)   // elevated surface
val SlateGray     = Color(0xFF2C3540)   // dividers / borders

// Neutrals - Light Mode Base
val OffWhite      = Color(0xFFF5F7FA)
val SoftWhite     = Color(0xFFECEFF4)
val LightSurface  = Color(0xFFDDE2EA)

// On-colors
val OnDark        = Color(0xFFE8EDF2)   // text on dark backgrounds
val OnLight       = Color(0xFF0D1117)   // text on light backgrounds
val OnPrimary     = Color(0xFF0D1117)   // text on green (dark for contrast)
val ErrorRed      = Color(0xFFFF4D4D)
val ErrorRedLight = Color(0xFFFFDAD6)